{
  "name": "prompt-manager",
  "version": "1.0.0",
  "description": "A comprehensive WordPress plugin for managing, publishing, and protecting AI-generated prompts with advanced NSFW content protection.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "start": "wp-scripts start src/index.js --output-path=build --output-filename=blocks.js",
    "build": "wp-scripts build src/index.js --output-path=build --output-filename=blocks.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^30.20.0",
    "jest": "^29.7.0"
  }
}
